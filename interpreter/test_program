program
{
 int a = 51, b = 6, c;
 real g = 1.5;
 string x = "abc", y, z = "abcd";
 /*c = (a + b) * 2;*/ if (c >= 100 or x == z)
 {
 read (y);
 write (y);
 write (b + g + + not - - + + a, c);
 }
 else
 c = a = 21;
 while (c > 100)
 {
 c = c - 5;
 z == z == 5 * a + g < b; a and c or a or b and c;
 continue;
 write (c);
 x = x + "step";
 }
 write (x);
 a = (b = (c = 1) + 1) + 1;
}
